# Mental Health AI Chatbot V2.0 - Python Dependencies

## Core Dependencies (Required)
requests>=2.31.0

## Advanced NLP Features (Optional - Recommended)
# Uncomment the ones you want to install:

# Transformer-based emotion detection (most accurate)
# transformers>=4.30.0
# torch>=2.0.0

# Sentiment analysis
# textblob>=0.17.1

# Advanced NLP (entity recognition, key phrases)
# spacy>=3.5.0

## Installation Instructions:

# 1. Minimal install (just LM Studio support):
#    pip install requests

# 2. Install with NLP features (recommended):
#    pip install requests transformers torch textblob spacy
#    python -m spacy download en_core_web_sm

# 3. If you get errors with torch, try CPU-only version:
#    pip install requests transformers torch --index-url https://download.pytorch.org/whl/cpu textblob spacy
#    python -m spacy download en_core_web_sm

# Note: sqlite3 is built-in with Python, no installation needed